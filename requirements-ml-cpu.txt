# Lightweight CPU-only inference stack for local dev / Codespaces.
# Pins also mirrored in constraints.txt for determinism.
faster-whisper==1.2.0
onnxruntime==1.22.1
sentencepiece==0.2.1
transformers==4.56.1
# Optional (handy for local audio I/O; skip in CI)
sounddevice==0.5.2

# NOTE: Intentionally NO torch here; it pulls multi-GB CUDA wheels.
# For GPU, see requirements-ml-gpu.txt.