# Aggregate requirements for local dev (LIGHTWEIGHT).
# ML stacks are now OPT-IN to avoid multi-GB downloads in Codespaces/laptops.
# See: requirements-ml-cpu.txt or requirements-ml-gpu.txt and Makefile targets.
-r requirements-ci.txt
-r requirements-dev.txt
# (intentionally no ML include here)
