layout python3
export VIRTUAL_ENV="$(pwd)/.venv"
if [ ! -d "$VIRTUAL_ENV" ]; then python -m venv "$VIRTUAL_ENV"; fi
source "$VIRTUAL_ENV/bin/activate"
export LX_OUT_DIR="${LX_OUT_DIR:-${GF_OUT_DIR:-$(pwd)/out}}"
# Migration note: GF_* is deprecated; use LX_* going forward.
